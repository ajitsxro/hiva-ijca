# pyproject.toml
[project]
name = "hotpotqa-finetune"
version = "0.1.0"
description = "Finetune DistilBERT on HotpotQA"
requires-python = ">=3.9"

dependencies = [
    "transformers>=4.40.0",
    "datasets>=2.19.0",
    "evaluate",
    "torch",
    "accelerate",
    "numpy>=2.0.2",
]
